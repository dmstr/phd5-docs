# Requiements


## Docker

### On your host-system

- `docker`
- `docker-compose`
- `git` (optional)
- `make` (optional)

Docker Beta for Mac or Windows

`docker-machine`
	
	docker-machine ls

## Non-dockerized installation